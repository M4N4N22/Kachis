Generated by `compact compile`. The `contract/` stub lets the console typecheck before a compile. After compile, this folder also contains `keys/` and `zkir/` (gitignored — too large). Judges: run the compile command in `compact/README.md` (Linux/macOS/WSL).
