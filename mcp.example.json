{
  "mcpServers": {
    "kachis-agent": {
      "command": "npx",
      "args": ["tsx", "src/index.ts"],
      "cwd": "${workspaceFolder}/agent",
      "env": {
        "KACHIS_CONSOLE_URL": "http://localhost:3000"
      }
    }
  }
}
